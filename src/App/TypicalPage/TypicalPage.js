import LoginForm from "./LoginForm/LoginForm";

const TypicalPage = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        margin: "0 auto",
      }}
    >
      <LoginForm />
    </div>
  );
};

export default TypicalPage;
